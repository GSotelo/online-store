import React from "react";

import {
    Flex,
    Text
} from "@chakra-ui/layout";

const EspritLogo = (props) => (
    <Flex>
        <Text variant="logo">ESPRIT</Text>
    </Flex>
);

export default EspritLogo;